import React from 'react';
import { Route, Switch } from 'react-router-dom'

import Nav from 'Nav'

var Main = (props) => {
  return (
    <div>
      <Nav />
      <Switch>
      </Switch>
    </div>
  );
}

export default Main;
